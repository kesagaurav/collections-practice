import java.util.List;
import java.util.ArrayList;
import java.util.Map;
import java.util.stream.Stream;
import java.util.HashMap;
import java.util.HashSet;

public class Main {

	public static void main(String[] args) {

		HashMap<Long, Long> sampleMap = new HashMap<>();
		sampleMap.put(2L, 2L);
		sampleMap.put(6L, 6L);
		sampleMap.put(3L, 3L);
		List<Long> sampleList = new ArrayList<>();
		sampleList.add(1L);
		sampleList.add(2L);
		sampleList.add(3L);
		sampleList.add(5L);
		int count = 0;
//		for (Map.Entry<Long, Long> entry : sampleMap.entrySet()) {
//			if(sampleList.contains(entry.getKey())) {
//				System.out.println("duplicateds are " + entry.getKey());
//			}
//		}
//		HashSet<Integer> set=new HashSet<>();
//		set.add(1);
//		
		HashMap<Integer,List> map=new HashMap<>();
		
		
	}

}
