import java.util.Arrays;

public class Main {

	public static void main(String[] args) {

		String str[]= {"flower","flow","flight"};
		PrefixString(str);
	}
	
	public static String PrefixString(String str[]) {
		String str1="";
		boolean b=Arrays.stream(str).anyMatch(str1::startsWith);
		for(int i=0;i<str.length-1;i++) {
			if(str1.startsWith(str[i])==str1.startsWith(str[i+1])) {
				str1=str[i];
				break;
			}
			return str1;

			
		}
		return "";
	 
		
	}

}
